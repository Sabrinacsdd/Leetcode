## Arrays method
```java
Arrays.copyOfRange(nums1, 0 ,k);
Arrays.sort(nums1);
Arrays.asList(nums[i], nums[l], nums[h]);
Arrays.fill(parent, -1);
Arrays.asList() This method takes the array a which is required to be converted into a List.
```