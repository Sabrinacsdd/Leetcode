## Integer method
```java
int Integer.valueOf(s.substring(i-1, i));//return the substring at i as an integer
int Integer.intValue(); //The method returns the numeric value which is represented by the object after conversion to the integer type.
 ```